#include <iostream>
#include <cmath>
using namespace std;

int main() {
    for (int i = 1; i <= 500; i++) {
        int num = i, sum = 0;
        while (num > 0) {
            int rem = num % 10;
            sum += rem * rem * rem;
            num /= 10;
        }
        if (sum == i)
            cout << i << " ";
    }
    return 0;
}
